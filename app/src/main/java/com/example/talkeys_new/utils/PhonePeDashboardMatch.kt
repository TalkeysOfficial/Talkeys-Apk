package com.example.talkeys_new.utils

// SECURITY: Production CLIENT_SECRET was previously stored here and has been removed.
// The exposed secret MUST be rotated in the PhonePe dashboard — see CURRENT_CLIENT_API_AUDIT.md.
// CLIENT_SECRET belongs on the backend server only, never in a mobile binary.

object PhonePeDashboardMatch {
    const val CLIENT_ID = PhonePeConfig.CLIENT_ID
}